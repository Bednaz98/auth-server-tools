generator client {
    provider = "prisma-client-js"
    output   = "../node_modules/@client-gen/feedback"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Testing {
    id String @id
}
